import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('settings overlay test', (widgetTester) async {
    // Working ✅
  });
  testWidgets('start game with each component', (widgetTester) async {
    // Working ✅
  });
  testWidgets('Game Over play again event', (widgetTester) async {
    // Working ✅
  });
  testWidgets('Game Over exit event', (widgetTester) async {
    // Working ✅
  });
  testWidgets('pause state', (widgetTester) async {
    // Working ✅
  });
  testWidgets('pause state with resume event', (widgetTester) async {
    // Working ✅
  });
  testWidgets('pause state with restart event', (widgetTester) async {
    // Working ✅
  });
  testWidgets('pause state with exit event and start again event',
      (widgetTester) async {
    // Working ✅
  });
  testWidgets(
    'pause state with exit event and'
    ' start game again with all component',
    (widgetTester) async {
      // Working ✅
    },
  );
  testWidgets(
    'high score caching',
        (widgetTester) async {
      // Working ✅
    },
  );
  testWidgets(
    'Sound Settings Check',
        (widgetTester) async {
      // Working ✅
    },
  );
  testWidgets(
    'Sound SFX Check ',
        (widgetTester) async {
      /// JUMP, HIT, ATTACK, DEAD, ButtonClick
      // Working ✅
    },
  );
}
